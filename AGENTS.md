# AGENTS.md — Context for Codex sessions in this repo

This file is read automatically by Codex when a session starts in this directory. Its job: hand the next session the full context so it can be productive immediately.

## What this project is

**formloop** lets an LLM agent generate a Typeform-quality form on-the-fly to ask a human structured questions, then resume execution when the form is submitted.

The thesis (developed during the night of 2026-05-09 with Jérôme):

1. Chat is a bad interface when an agent needs more than 2-3 structured inputs from a human.
2. `AskUserQuestion`-style harness primitives are too limited (4×4 max, no rich types).
3. Tally/Typeform aren't designed to be generated by code or to embed rich HTML context.
4. There's a real, growing pain point as agentic LLM usage spreads: how does an agent ask a human a complex question? How does the human stay in the loop without having to babysit the chat?

formloop is a **thin layer between any LLM agent and a human in the loop**, with a UI that goes beyond chat or markdown — embeddable HTML, clickable mockup grids, drag-and-drop, sliders with live previews.

## Origin story

This started as a discussion about Thariq Shihipar's article "The Unreasonable Effectiveness of HTML" (https://thariqs.github.io/html-effectiveness). Jérôme noticed that he often used Tally to communicate with Codex rather than typing in the terminal — a pattern that revealed a structural issue with chat-as-only-interface.

We then built (in one night):
1. A Vercel webhook bridge for Tally submissions
2. The custom Typeform-clone form engine (10 question types)
3. A Python skill (`ask-form`) that orchestrates the create → notify → poll → resume flow
4. This OSS-ready repo (where you are now)

After validating the concept end-to-end, Jérôme decided to turn it into an OSS project to (a) build dev credibility around AI tooling, (b) take a wedge in an emerging space before Anthropic/OpenAI absorb it natively.

## Repo structure

```
formloop/
├── apps/
│   └── server/          ← Next.js 16 app (form engine + API endpoints)
│                          Vercel project: `manibors-projects/formloop`.
│                          (Renamed from `tally-bridge` on 2026-05-13.)
├── packages/
│   ├── sdk-python/      ← Python driver `ask_form.py` (create, wait, cleanup)
│   └── sdk-typescript/  ← TODO — TypeScript SDK for Node/browser agents
├── examples/
│   ├── Codex/        ← TODO — example wiring as Codex skill
│   ├── openai-assistants/  ← TODO
│   ├── langchain/          ← TODO
│   └── vercel-ai-sdk/      ← TODO
├── docs/                ← TODO — architecture, types reference, integration guides
├── AGENTS.md            ← This file
├── README.md            ← Public pitch + quickstart
├── ROADMAP.md           ← Path from POC to OSS launch (4-6 weeks)
├── CONTRIBUTING.md      ← TODO
└── LICENSE              ← MIT
```

## What works today (2026-05-09)

- **`apps/server/`** — fully working Next.js app. Deployed on Vercel under `manibors-projects/tally-bridge` at `https://tally-bridge-manibors-projects.vercel.app`. Includes:
  - `POST /api/forms` — create a custom form (auth: shared secret)
  - `GET  /forms/[id]` — render the form (Typeform-clone UI)
  - `POST /api/forms/[id]/submit` — receive submissions
  - `GET  /api/response/[id]` — poll for response (auth: shared secret)
  - `POST /api/webhook` — legacy Tally webhook receiver (still works)
  - Storage: Upstash Redis (Vercel Marketplace integration), TTL 1h on responses, 24h on specs.
  - 10 question types, all tested end-to-end via `apps/server/test-e2e.sh`.

- **`packages/sdk-python/ask_form.py`** — working CLI:
  - `create-custom --spec '{...}'` → creates a form (ephemeral or persistent), returns `{form_id, form_url, persistent}`
  - `create --spec '{...}'` → legacy Tally form mode (still functional, kept for now)
  - `wait --form-id <id> [--timeout 1800]` → polls for a single ephemeral response, returns answers
  - `list-submissions --form-id <id>` → list all submissions of a persistent form (JSON)
  - `export-csv --form-id <id> [--output path]` → CSV export of a persistent form
  - `wait-n --form-id <id> --n N [--timeout 1800]` → poll a persistent form until at least N submissions land
  - `cleanup --form-id <id>` → deletes the form (Tally only)
  - URL + secret overridable via `FORMLOOP_BRIDGE_URL` / `FORMLOOP_WEBHOOK_SECRET` env vars.

- **Demo URL** (live for ~24h after creation, valid until 2026-05-10):
  `https://tally-bridge-manibors-projects.vercel.app/forms/PG3cmELG6d`

## What's missing for OSS launch

See `ROADMAP.md` for the full plan. High-level:

1. **Decoupling**: today the Python SDK has the bridge URL + secret hardcoded. Needs ENV-based config.
2. **TypeScript SDK** (`packages/sdk-typescript/`) — npm package `formloop` for Node + browser.
3. **Self-hostable in 1 command**: Docker compose for the server + Redis (currently requires Vercel + Upstash setup).
4. **Examples** for the major agent frameworks (OpenAI Assistants, LangChain, Vercel AI SDK, MCP).
5. **Documentation site**: probably under `docs/`, or a separate docs framework (Nextra, Mintlify).
6. **Public demo**: stable hosted URL on `formloop.dev` or similar (currently `tally-bridge-manibors-projects.vercel.app`).
7. **Branding**: logo, landing page, GIF demo for the README.
8. **Tests**: today only an E2E shell script. Need Vitest/Jest for the React components and Python tests for the SDK.
9. **CI**: GitHub Actions for lint + tests + build on every PR.
10. **Launch materials**: Twitter thread, Show HN post, blog article.

## Architecture (current)

```
LLM agent (Codex, GPT, etc.)
  │
  ├── 1. python ask_form.py create-custom --spec '{...}'
  │       → POST /api/forms (server)
  │       → server stores spec in Upstash Redis under spec:{id} (TTL 24h)
  │       → returns { form_id, form_url }
  │
  ├── 2. agent sends notification (email/Slack/Telegram) with form_url
  │
  ├── 3. Human opens form_url in browser
  │       → GET /forms/[id] (server-rendered Next.js page)
  │       → Loads Typeform-clone client component
  │       → Human fills form, clicks Submit
  │       → POST /api/forms/[id]/submit
  │       → Server stores response in Redis under response:{id} (TTL 1h)
  │
  └── 4. python ask_form.py wait --form-id <id>
          → polls GET /api/response/[id] every 5s
          → when ready, returns structured answers (label-keyed dict)
```

The legacy Tally path (form created on tally.so, webhook from Tally → server) writes to the same `response:{id}` key, so the polling logic is unchanged.

## Tech stack

**Server (`apps/server/`):**
- Next.js 16 (App Router, Turbopack)
- React 19
- Tailwind CSS v4 (CSS-based config in `app/globals.css`)
- Framer Motion (page transitions)
- @dnd-kit (drag-and-drop for the `rank` type)
- isomorphic-dompurify (HTML sanitization)
- @upstash/redis (storage)
- Lucide React (icons)
- TypeScript strict

**SDK (`packages/sdk-python/`):**
- Python 3.10+
- stdlib only (urllib, json, argparse) — zero external deps

## Key design decisions

1. **Redis as ephemeral store, not a DB.** Responses TTL 1h, specs TTL 24h. No history, no persistence. Privacy-by-design + simplicity. Adding history is opt-in.

2. **Same `response:{id}` key for Tally and custom forms.** The polling endpoint serves both modes transparently. Migration path: agents can move from Tally mode to custom mode without changing their polling logic.

3. **HTML sanitization on the server.** Any HTML embedded by the agent (`html` blocks, `html-pick` options, `scale-preview` templates) goes through DOMPurify. No `<script>`, no event handlers, no iframes. SVG is allowed (charts, illustrations).

4. **Typeform-style UX deliberately copied.** One question per screen, vertical-slide transitions, large generous typography, accent-color (#6366f1) for selection. Familiar to anyone who's filled a Typeform.

5. **Keyboard shortcuts as first-class.** `a/b/c` to pick options, digits for scales, `o/n` for yes/no, arrows to navigate, Enter to advance. Power users feel at home.

6. **Mobile-first.** All views are responsive, drag-and-drop uses TouchSensor, scale-preview slider uses native range input.

## Conventions

- **Commits**: descriptive messages, "what + why", co-author credit when Codex is involved.
- **TypeScript**: `strict: true`, no `any`, prefer discriminated unions for question types.
- **Components**: server components by default; mark client components explicitly with `"use client"`.
- **Styling**: prefer CSS variables (in `globals.css`) and inline styles for one-off layout. Avoid Tailwind utility soup; the design tokens are in CSS.
- **Imports**: absolute paths via `@/` alias (configured in `tsconfig.json`).

## How to run locally (today)

```bash
# Server
cd apps/server
pnpm install   # or npm install
# Need ENV: WEBHOOK_SECRET, KV_REST_API_URL, KV_REST_API_TOKEN
# Currently: pull from Vercel with `vercel env pull` if linked
pnpm dev       # http://localhost:3000

# Python SDK
cd packages/sdk-python
python ask_form.py --help
# Defaults to the production formloop URL + secret, but both are overridable:
#   FORMLOOP_BRIDGE_URL=https://...   FORMLOOP_WEBHOOK_SECRET=...   python ask_form.py ...
```

## How to deploy (today)

The `apps/server/` directory is linked to the Vercel project
`manibors-projects/formloop` (renamed from `tally-bridge` on 2026-05-13 — the
old `tally-bridge.vercel.app` alias is preserved for now). The Upstash Redis
integration was carried over by the rename, no env migration needed.

```bash
cd apps/server
vercel deploy --prod
```

After the next prod deploy, the canonical URL becomes `formloop.vercel.app`.

## Personal context (Jérôme)

- **Jérôme Borenstejn** — French, Manibor (his consulting/dev structure). Email: `jerome@manibor.com`.
- He uses Codex daily, has a vault Obsidian-based "second brain", and several side projects (Münzer 2026 campaign, Simundia, CertiDeal, etc.).
- His goal with formloop: build dev/AI credibility through a viral OSS project (target: 1000+ stars in the first month post-launch).
- He's not full-time on this — he'll work on it on the side while running his other commitments.

## When to ask the human vs. decide alone

- **Decide alone**: code organization, naming inside the codebase, dep choices that match the existing stack, refactors, tests, documentation phrasing.
- **Ask Jérôme**: anything that touches the public API surface, the project name/branding, the launch strategy, the timing, or that costs money.

## Useful pointers

- **Vercel deployment**: `https://tally-bridge.vercel.app` (legacy alias, still active) → will become `https://formloop.vercel.app` on next prod deploy
- **Vercel project**: `manibors-projects/formloop` (renamed from `tally-bridge` on 2026-05-13)
- **Upstash Redis dashboard**: `https://vercel.com/d/dashboard/integrations/upstash/icfg_vmuw26bcg9RrwXh5TzUrbwYN/resources/store_GLsTGBeDgPQpQef0`
- **Original Tally token** (used by legacy path): inside `~/.Codex/skills/tally-forms/scripts/tally.py`
- **Local skill**: `~/.Codex/skills/ask-form/` (still works against the live deployment)
- **Inspiration article**: https://thariqs.github.io/html-effectiveness — read this before working on the project; it explains why we're here.
- **Closest competitor**: HumanLayer (humanlayer.dev) — focused on orchestration/delivery, not UI. We could integrate rather than compete.
